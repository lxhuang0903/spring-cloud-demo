server:
  port: 9100
spring:
  application:
    name: order-service
  cloud:
    config:
      discovery:
        enabled: false
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/